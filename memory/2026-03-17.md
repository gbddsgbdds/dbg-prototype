# 2026-03-17 开发日志

## 自动开发任务：角色系统基础架构

### 需求
从 TODO P0 任务"更多角色设计"拆分出基础架构任务，为后续角色开发铺平道路。

### 实现内容
1. **类型定义** (`src/game/types.ts`)
   - 新增 `CharacterDef` 接口
   - 更新 `GamePhase` 添加 `'character_select'`

2. **角色定义** (`src/data/characters.ts`) - 新文件
   - 定义默认角色"心素"（李火旺）
   - 导出 `ALL_CHARACTERS` 数组
   - 提供 `getCharacterById` 工具函数

3. **卡牌查找** (`src/data/cards.ts`)
   - 新增 `getCardById` 和 `getCardsByIds` 函数
   - 支持根据ID列表获取卡牌定义

4. **状态管理重构** (`src/game/store.ts`)
   - `mkPlayer` 接受 `CharacterDef` 参数
   - `newGame` 接受可选 `characterId` 参数
   - 新增 `selectCharacter` 方法
   - 初始卡组从角色配置生成
   - 存档包含 `characterId`

5. **角色选择组件** (`src/components/CharacterSelect.tsx`) - 新文件
   - 显示可用角色列表
   - 显示角色属性（HP、理智、能量、卡牌数）
   - 点击选择角色开始游戏

6. **App.tsx 更新**
   - 开始游戏流程：开始界面 → 角色选择 → 地图
   - 继续游戏：直接从存档恢复

### 验证
- `tsc --noEmit` ✅
- `vite build` ✅

### 改动量
- 新增文件: 3
- 修改文件: 4
- 新增代码: ~350行

### 后续建议
- 实现法教角色（高HP、低理智、自残流）
- 实现坐忘道角色（低HP、高理智、理智回复流）
- 角色专属卡牌设计