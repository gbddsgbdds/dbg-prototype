# 2026-03-17 开发日志

## 角色解锁系统实现

### 完成内容
1. **角色解锁逻辑**
   - 在 meta.ts 添加 `unlockCharacter` 方法
   - 添加 `CharacterUnlockPopup` 类型和弹窗队列
   - 心素通关一次解锁法教角色
   - 入魔状态下击败Boss解锁坐忘道角色

2. **角色选择界面更新**
   - 检查 `unlockedCharacters` 显示锁定/解锁状态
   - 锁定角色显示锁定图标和解锁条件
   - 锁定角色不可点击

3. **角色解锁弹窗**
   - 新增 `CharacterUnlockPopup.tsx` 组件
   - 金色边框、角色图标、角色名称
   - 自动显示在成就弹窗之后

4. **验收**
   - TypeScript 检查通过
   - 构建成功
   - 单元测试全部通过（33个测试）
   - 测试日志显示角色解锁逻辑生效

### 新增/修改文件
- `src/game/meta.ts` - 添加角色解锁逻辑
- `src/components/CharacterSelect.tsx` - 锁定状态显示
- `src/components/CharacterSelect.css` - 锁定样式
- `src/components/CharacterUnlockPopup.tsx` - 解锁弹窗
- `src/components/VictoryScreen.tsx` - 传递 isMadnessVictory 参数
- `src/App.tsx` - 导入解锁弹窗组件
- `src/App.css` - 解锁弹窗样式
- `docs/feat-character-unlock.md` - 需求文档

### 设计决策
- 法教解锁条件：使用心素通关一次（新手友好）
- 坐忘道解锁条件：入魔状态下击败Boss（体现"以疯证道"主题）

---

## E2E 测试框架实现

### 完成内容
1. **Playwright 集成**
   - 安装 @playwright/test 依赖
   - 创建 playwright.config.ts 配置文件
   - 创建 e2e/ 目录和测试文件

2. **测试用例** (6 个全部通过)
   - 开始界面显示正常
   - 开始游戏 → 角色选择
   - 角色选择 → 进入游戏
   - 成就界面可以打开和关闭
   - 音效设置界面可以打开和关闭
   - 移动端响应式布局测试

3. **Bug 修复**
   - 修复 App.tsx 中 useState/useMetaStore 在条件语句内部的 hooks 规则违规
   - 添加 'title' 阶段到 GamePhase 类型，修复初始状态问题

### 新增文件
- `playwright.config.ts` - Playwright 配置
- `e2e/game-flow.spec.ts` - E2E 测试用例

### 新增 npm scripts
- `test:e2e` - 运行 E2E 测试
- `test:e2e:ui` - E2E 测试 UI 模式
- `test:e2e:debug` - E2E 测试调试模式

### 下一步建议
- CI/CD 自动化（GitHub Actions）
- 增加更多战斗流程测试用例
- 添加性能测试