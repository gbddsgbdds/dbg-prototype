# 2026-03-17 开发日志

## 自动开发任务：角色系统基础架构

### 需求
从 TODO P0 任务"更多角色设计"拆分出基础架构任务，为后续角色开发铺平道路。

### 实现内容
1. **类型定义** (`src/game/types.ts`)
   - 新增 `CharacterDef` 接口
   - 更新 `GamePhase` 添加 `'character_select'`

2. **角色定义** (`src/data/characters.ts`) - 新文件
   - 定义默认角色"心素"（李火旺）
   - 导出 `ALL_CHARACTERS` 数组
   - 提供 `getCharacterById` 工具函数

3. **卡牌查找** (`src/data/cards.ts`)
   - 新增 `getCardById` 和 `getCardsByIds` 函数
   - 支持根据ID列表获取卡牌定义

4. **状态管理重构** (`src/game/store.ts`)
   - `mkPlayer` 接受 `CharacterDef` 参数
   - `newGame` 接受可选 `characterId` 参数
   - 新增 `selectCharacter` 方法
   - 初始卡组从角色配置生成
   - 存档包含 `characterId`

5. **角色选择组件** (`src/components/CharacterSelect.tsx`) - 新文件
   - 显示可用角色列表
   - 显示角色属性（HP、理智、能量、卡牌数）
   - 点击选择角色开始游戏

6. **App.tsx 更新**
   - 开始游戏流程：开始界面 → 角色选择 → 地图
   - 继续游戏：直接从存档恢复

### 验证
- `tsc --noEmit` ✅
- `vite build` ✅

### 改动量
- 新增文件: 3
- 修改文件: 4
- 新增代码: ~350行

### 后续建议
- 实现法教角色（高HP、低理智、自残流）
- 实现坐忘道角色（低HP、高理智、理智回复流）
- 角色专属卡牌设计
## 2026-03-17 05:30 - 自动开发任务

### 完成
- 开始界面背景粒子动画
  - 添加 8 个浮动光点粒子，紫色/红色配色
  - CSS 纯动画实现，性能友好
  - 通过 tsc + vite build 验证

### 改动文件
- src/App.tsx - 添加粒子容器
- src/App.css - 添加粒子动画样式
- docs/TODO.md - 更新任务进度

## 2026-03-17 05:47 - 自动开发任务

### 需求
角色选择界面移动端深度适配（400px及以下）

### 分析
- CharacterSelect.css 只有 768px 断点
- 小屏幕手机（375px）上体验不佳
- 需要补充 400px 及以下的深度适配

### 实现内容
1. **400px 及以下媒体查询**
   - 调整标题、副标题字体大小
   - 调整角色卡片内边距
   - 调整角色图标、名称、描述尺寸
   - 调整属性网格间距
   - 调整选择按钮尺寸（min-height: 44px 触控友好）

2. **触控设备优化**
   - 添加触控设备专用媒体查询
   - 移除 hover 效果（避免触控设备卡在 hover 状态）
   - 添加 :active 状态反馈
   - 确保按钮触控区域 ≥ 44px

### 验证
- `npx tsc --noEmit` ✅
- `npx vite build` ✅

### 改动文件
- src/components/CharacterSelect.css
- docs/TODO.md
- docs/feat-character-select-mobile-optimization.md（需求文档）

### 后续建议
- 继续完善其他界面的响应式布局
- 性能优化（如减少重绘、懒加载等）

